@using OnlineStore.Models

@{
    Layout = null;
}
<select name="subName" id="s"  class="setFont">
    <option value="0">-ALL-</option>
    @foreach (var s in Model)
    {

        <option value=@s.s_Id class="setFont">@s.subName</option>

    }
</select>